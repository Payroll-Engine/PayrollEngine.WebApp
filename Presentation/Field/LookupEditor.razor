@using ValueType = PayrollEngine.ValueType
@using PayrollEngine.WebApp.ViewModel

@* unsupported value types *@
@if (Field.ValueType is ValueType.NumericBoolean or ValueType.WebResource or ValueType.None)
{
    return;
}

@if (Field.ValueType.IsString())
{
    <StringLookup Field="@Field" Language="@Language" Disabled="@Disabled" />
}
else if (Field.ValueType.IsBoolean())
{
    <BooleanLookup Field="@Field" Language="@Language" Disabled="@Disabled" />
}
else if (Field.ValueType.IsInteger())
{
    <IntegerLookup Field="@Field" Language="@Language" Disabled="@Disabled" />
}
else if (Field.ValueType.IsDecimal())
{
    <DecimalLookup Field="@Field" Language="@Language" Disabled="@Disabled" />
}
else if (Field.ValueType.IsDateTime())
{
    <DateTimeLookup Field="@Field" Language="@Language" Disabled="@Disabled" />
}

@code
{
    [Parameter] public IFieldObject Field { get; set; }
    [Parameter] public Language Language { get; set; }
    [Parameter] public bool Disabled { get; set; }
}
