@inherits FieldEditorBase;

<MudSwitch T="bool?"
           @bind-Checked="@Field.ValueAsBoolean"
           Culture="@EditCulture"
           Color="Color.Primary"
           Label="@ValueLabel"
           Error="@Error"
           Disabled="@Disabled"
           ReadOnly="@ReadOnly"
           Required="@Required"
           RequiredError="@ValueRequiredError" />

@code
{
    [Parameter]
    public LabelPosition LabelPosition { get; set; } = LabelPosition.Start;
}