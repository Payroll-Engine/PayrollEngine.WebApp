@inherits FieldEditorBase;

<MudCheckBox T="bool?"
             @bind-Checked="@Field.ValueAsBoolean"
             Culture="@Culture"
             Color="Color.Primary"
             Label="@ValueLabel"
             LabelPosition="LabelPosition.Start"
             Disabled="@Disabled"
             ReadOnly="@ReadOnly"
             Required="@Required"
             Error="@Error" />
