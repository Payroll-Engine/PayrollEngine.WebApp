@typeparam T where T : struct, Enum
@inherits EnumItemsBase<T>

@foreach (var value in Values)
{
    @* Use the enum name as list item value *@
    <MudListItem T="string" Value="Enum.GetName(value.Value)">@value.Label</MudListItem>
}
