@using PayrollEngine.WebApp.Shared
@using System.Globalization

@typeparam T
@inherits NullableEnumItemsBase<T>

@foreach (var value in Values)
{
    <MudSelectItem T="T" Value="@value.Value">@value.Label</MudSelectItem>
}
