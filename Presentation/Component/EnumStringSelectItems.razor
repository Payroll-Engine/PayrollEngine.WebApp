@using PayrollEngine.WebApp.Shared

@typeparam T where T : struct, Enum
@inherits EnumItemsBase<T>

@foreach (var value in Values)
{
    <MudSelectItem T="string" Value="@value.Value.ToString()">@value.Label</MudSelectItem>
}
